import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_tema3_ejercicios',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
